---
'@pymodel/pythinker-code': minor
'@pymodel/pythinker-code-sdk': patch
---

Rebuild telemetry as a scope-bound layered context registry so session, agent, and turn fields flow into every event without a separate agent telemetry context service.
