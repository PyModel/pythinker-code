---
"@pymodel/pythinker-code": minor
---

The model you pick in the model picker is now remembered and restored when your default model becomes unavailable, and equally capable models now prefer the newer version.
